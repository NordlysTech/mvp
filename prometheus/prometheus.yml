global:
  scrape_interval: 5s  # How often to scrape targets by default.

scrape_configs:
  - job_name: 'flask_app'
    scrape_interval: 5s  # Override default for this job.
    static_configs:
      - targets: ['solvi_mvp:5000']
