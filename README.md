# mvp


## Getting Started


**Build the Docker image:**

```bash
docker-compose build
```

**Run all services with a single command:**

```bash
docker-compose up -d
```

**Stop the services:**

```bash
docker-compose down
```